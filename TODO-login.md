# Login Route Integration

## Steps (0/3)

- [x] 1. Create `src/app/api/auth/route.ts` POST {email, password} → verify creds + sendLoginCode
- [x] 2. Update `src/app/auth/login/page.tsx` → single fetch('/api/auth/', {email, password})
- [x] 3. Test + complete

**Login route integration complete!**

**Next: Create api/auth/route.ts**

